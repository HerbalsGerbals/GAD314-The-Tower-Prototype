using UnityEngine;

public class BuildingManager : MonoBehaviour
{
    [SerializeField] private BuildingTypeSO activeBuildingType; //Current Building
    [SerializeField] private Transform buildingPrefab; 

    // Update is called once per frame
    void Update()
    {
        
    }
}
